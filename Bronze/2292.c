#include <stdio.h>

int main()
{
    int n, com, six = 0;
    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
    {
        for (int j = i; j <= i * 6 + 1; j++)
        {
            
        }
    }
    printf("%d\n", six);
}