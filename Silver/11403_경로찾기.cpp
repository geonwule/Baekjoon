#include <iostream>

int arr[100][100];
int main()
{
    int n;
    for (int y = 0; y < n; y++)
    {
        for (int x = 0; x < n; x++)
        {
            std::cin >> arr[y][x];
        }
    }
}